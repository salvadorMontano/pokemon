<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pokemon">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pokemon">
        <activity
            android:name=".ui.view.PokemonAbilitiesActivity"
            android:exported="false" />
        <activity
            android:name=".ui.view.PokemonEvolutionaryLineActivity"
            android:exported="false" />
        <activity
            android:name=".ui.view.PokemonDetailActivity"
            android:exported="false"
            android:label="@string/title_activity_pokemon_detail"
            android:theme="@style/Theme.Pokemon.NoActionBar" />
        <activity
            android:name=".ui.view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>